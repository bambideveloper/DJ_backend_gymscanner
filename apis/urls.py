from django.urls import path
from . views import *

urlpatterns = [
    # User API (Create, Read, Update, Delete)
    # path('insert', user_insert, name = 'user_insert'),
    # path('delete/<int:pk>', user_delete, name = 'user_delete'),
    # path('update/<int:pk>', user_update, name = 'user_update'),
]